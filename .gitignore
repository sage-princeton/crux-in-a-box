telegram_bots.json
linux/placeholders*.txt
!linux/placeholders.txt.example
!linux/placeholders-codex.txt.example
# filled placeholder configs hold live keys — ignore them wherever they land
placeholders*.txt
# scrub blacklists are BUILT FROM live secrets — never commit them
blacklist*.txt

**.pyc
INVENTORY.md
.DS_Store

**gog-home.tar.gz

clean-room

# local diagnostics / caches — never commit
*.jsonl
*.zip
# raw run telemetry holds live credentials — only the _CLEAN versions may be committed
personas_telemetry.jsonl
tabpfn_telemetry.jsonl
.mypy_cache/
.claude/settings.local.json
