telegram_bots.json
linux/placeholders*.txt
!linux/placeholders.txt.example

# src/ec2-* provisioning config: real values stay local, .example is tracked
src/**/placeholders*.txt
!src/**/placeholders*.txt.example

# Local secrets bundles; example files remain tracked.
src/**/*secrets*.json
!src/**/*secrets*.json.example

**.pyc
INVENTORY.md
.DS_Store

**gog-home.tar.gz

clean-room

# local diagnostics / caches — never commit
*.jsonl
*.zip
# raw run telemetry holds live credentials — only the _CLEAN versions may be committed
personas_telemetry.jsonl
tabpfn_telemetry.jsonl
.mypy_cache/
.claude/settings.local.json
.claude/worktrees/
CLAUDE.local.md

# local env files hold live API keys — never commit (rotate the key if one leaks)
.env
.env.*

# papers fed to verifier-calibration are review inputs, not repo source
/*.pdf

# scrubbed run records pulled by utils/export-run.sh — local diagnostics, never commit
runs-export/

clean-room-old
logs-for-release
