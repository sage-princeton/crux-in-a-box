Produce a research paper answering the research question in AGENTS.md — {{RESEARCH_QUESTION}} — at a quality an expert human reviewer would grade as publishable at {{VENUE|NeurIPS}}. Verified by: paper/paper.pdf compiling from source and passing scripts/gate_artifact.sh (page and abstract caps, figures, hygiene); every headline claim meeting AGENTS.md § The bar with evidence artifacts on disk; and the isolated referee reports in VERIFIER_FEEDBACK.md trending toward acceptance. Preserve throughout: the evidence rules and red lines in AGENTS.md — never fabricate results, never edit the outer loop or verifier state. Boundaries: this workspace, the open web, RunPod GPU compute within budget, and one refine.ink review credit. Maintain PLAN.md at the repo root: an allocation of all three budgets — the hours until the deadline (by phase, with absolute UTC checkpoints), the API budget, and the GPU budget — sized so the full budgets are deployed by the deadline, with experiments complete before the final-polish window. Every iteration, reconcile actual spend and clock (scripts/budget_status.sh) against the plan's current checkpoint: either you are on plan, or you log a one-line revision in LOG.md with the reason and update PLAN.md. Reaching the deadline with large unspent budgets and a below-bar paper is a failure of this goal — the plan exists to prevent it, not to document it. Between turns, follow the AGENTS.md iteration protocol: commit, append the LOG.md hand-off, and treat the verifier's priorities as the default agenda. Mark this goal complete only when the full bar is met per evidence on disk; mark it blocked only if the same blocker survives three consecutive turns with no route around it. Completing or blocking the goal does not end the run — the outer loop owns termination and will continue launching iterations until the API budget or clock is exhausted.
