# Merge these values into the controller's existing environment in SSM.
# This is not a complete replacement for the controller environment.
AGENTRQ_SLACK_APP_ID=<slack-app-id>
AGENTRQ_SLACK_CLIENT_ID=<slack-client-id>
AGENTRQ_SLACK_CLIENT_SECRET=<slack-client-secret>
AGENTRQ_SLACK_SIGNING_SECRET=<slack-signing-secret>
